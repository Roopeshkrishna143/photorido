# Photorido Admin

This directory is a reserved scaffold for the future Photorido admin dashboard.

Current status:

- No UI has been implemented yet
- The placeholder build creates a minimal `dist/` artifact so CI can validate the monorepo structure
- When the admin dashboard is started, this folder can be upgraded into its own React or Next.js app without affecting the frontend or backend apps
